services:
  backend:
    build:
      context: ./.NET/ChristBackend/ChristBackend
    ports:
      - "8080:8080"

  frontend:
    build:
      context: ./Angular/ChristFrontend
    ports:
      - "80:80"
    depends_on:
      - backend